<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="icon" type="image/png" href="/logo SISRAM_20250323_112139_0000.png">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <style>
        body {
            background-color: #020520;
            padding: 0; /* Removido o padding */
            margin: 0;
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
        }

        thead, tr, th, h1, p {
            color: #ffffff;
        }

        table {
            text-align: center;
            width: 100%;
            max-width: 100%;
            margin-top: 10px;
        }

        h1 {
            font-size: 40px;
            margin-top: 5px;
        }

        p {
            font-size: 20px;
            margin-bottom: 10px;
        }

        .table-container {
            width: 100%;
            max-width: 100vw;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            margin-bottom: 15px;
        }

        /* Responsive styles */
        @media (max-width: 1024px) {
            h1 {
                font-size: 32px;
            }
            
            p {
                font-size: 18px;
            }
            
            table {
                font-size: 14px;
            }
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 28px;
                margin: 5px 0; /* Ajustado o margin */
            }
            
            p {
                font-size: 16px;
                margin: 5px 0; /* Ajustado o margin */
            }
            
            table {
                font-size: 12px;
                width: 100vw; /* Usando toda a largura da viewport */
                margin-left: -10px; /* Compensando o padding natural do body */
                margin-right: -10px;
            }
            
            img {
                max-width: 80px !important;
            }
        }
        
        @media (max-width: 480px) {
            h1 {
                font-size: 24px;
                margin: 3px 0; /* Reduzido ainda mais o margin */
            }
            
            p {
                font-size: 14px;
                margin: 3px 0; /* Reduzido ainda mais o margin */
            }
            
            table {
                font-size: 10px;
                width: 100vw; /* Usando toda a largura da viewport */
                margin-left: -10px; /* Compensando o padding natural do body */
                margin-right: -10px;
            }
            
            th, td {
                padding: 2px 1px; /* Reduzido o padding das células */
                white-space: nowrap;
            }
            
            img {
                max-width: 50px !important;
            }
            
            button {
                padding: 6px 10px;
                font-size: 12px;
                display: inline-block;
                margin: 5px;
            }
        }
    </style>
</head>
<body>
    <h1><%= title %></h1>
    <p>Atestados Cadastrados no sistema</p>

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Matrícula</th>
                    <th>Nome</th>
                    <th>Data de Entrega</th>
                    <th>Data de Afastamento</th>
                    <th>Período</th>
                    <th>Motivo</th>
                    <th>Turma</th>
                    <th>Imagem</th>
                </tr>
            </thead>
            <tbody>
                <% atestados1ads.forEach(function(atestados) { %>
                    <tr>
                        <td><%= atestados.id %></td>
                        <td><%= atestados.matricula %></td>
                        <td><%= atestados.nome %></td>
                        <td><%= new Date(atestados.data_entrega).toISOString().split('T')[0] %></td>
                        <td><%= new Date(atestados.data_afastamento).toISOString().split('T')[0] %></td>
                        <td><%= atestados.periodo %></td>
                        <td><%= atestados.motivo %></td>
                        <td><%= atestados.turma %></td>
                        <td>
                            <a href="/imagem/<%= atestados.id %>" target="_blank">
                                <img src="/imagem/<%= atestados.id %>" alt="Atestado" style="width: 100px; height: auto;">
                            </a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <button><a href="/serie_prof">Voltar</a></button>
</body>
</html>
